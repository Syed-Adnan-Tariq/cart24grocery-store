<?php

use Illuminate\Database\Seeder;

class RecipeOrdersTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
       // factory(App\OrderRecipe::class,10)->create();
/*       $recipeorder = new\App\OrderRecipe([

            'recipe_id' => '5',
            'order_id' => '3',
        ]);

        $recipeorder -> save();

        $recipeorder = new\App\OrderRecipe([

            'recipe_id' => '1',
            'order_id' => '2',
        ]);

        $recipeorder -> save();

        $recipeorder = new\App\OrderRecipe([

            'recipe_id' => '2',
            'order_id' => '3',
        ]);

        $recipeorder -> save();

        $recipeorder = new\App\OrderRecipe([

            'recipe_id' => '4',
            'order_id' => '3',
        ]);

        $recipeorder -> save();

        $recipeorder = new\App\OrderRecipe([

            'recipe_id' => '2',
            'order_id' => '4',
        ]);

        $recipeorder -> save();

 */       }
}
